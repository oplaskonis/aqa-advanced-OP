const log = console.log;

const number = 6;

for (let i = 1; i <= 10; i++) {
    log (`${number} * ${i} = ${number * i}`);
}

let n = 1; 
while (n <= 10) {
    log (`${number} * ${n} = ${number * n}`);
    n++;
}